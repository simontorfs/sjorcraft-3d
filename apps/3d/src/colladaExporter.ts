import { Viewer } from './viewer';
// create a collada file from the scene do NOT use the ColladaExporter class, create your own
// use the xmlbuilder package to create the xml file
// https://www.npmjs.com/package/xmlbuilder
export function createCollada(viewer: Viewer): string {
  return '';
}

